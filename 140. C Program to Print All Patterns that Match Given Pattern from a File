#include <stdio.h>
#include <string.h>
int main() { 
char pattern[50], line[100];
scanf("%s", pattern); FILE
*fp=fopen("file.txt","r");
while(fgets(line,100,fp)!=NULL)
if(strstr(line,pattern))
printf("%s", line); fclose(fp);
return 0;
}
