#include <stdio.h>
#include <string.h>
int main() {
char str[100]; scanf("%s", str);
int freq[256]={0};
for(int i=0;i<strlen(str);i++) freq[str[i]]++;
for(int i=0;i<strlen(str);i++) if(freq[str[i]]==1) { printf("%c\n",
str[i]); break; }
return 0;
}
