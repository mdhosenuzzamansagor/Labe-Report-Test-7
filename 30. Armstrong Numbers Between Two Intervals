#include <stdio.h>
#include <math.h>
int main(){
 int low,high,num,temp,rem,sum,d;
 scanf("%d %d",&low,&high);
 for(num=low;num<=high;num++){
 d=0; sum=0;
 for(temp=num;temp!=0;temp/=10){ d++; }
 for(temp=num;temp!=0;temp/=10){ rem=temp%10; sum+=pow(rem,d); }
 if(sum==num) printf("%d ",num);
 }
 return 0;
}
