#include <stdio.h>
int main() {
int n; scanf("%d", &n);
int a[n][n], trace=0, norm=0;
for(int i=0;i<n;i++) for(int j=0;j<n;j++) { scanf("%d", &a[i][j]);
norm+=a[i][j]*a[i][j]; if(i==j) trace+=a[i][j]; }
printf("Trace = %d, Norm = %d\n", trace, norm);
return 0;
}
