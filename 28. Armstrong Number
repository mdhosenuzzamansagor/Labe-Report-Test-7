#include <stdio.h>
#include <math.h>
int main(){
 int n,temp,r,sum=0,d=0; scanf("%d",&n);
 for(temp=n;temp!=0;temp/=10){ d++; }
 for(temp=n;temp!=0;temp/=10){ r=temp%10; sum+=pow(r,d); }
 if(sum==n) printf("Armstrong"); else printf("Not Armstrong");
 return 0;
}
