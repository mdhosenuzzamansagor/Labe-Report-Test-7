#include <stdio.h>
int main(){
 int n,i; long long t1=0,t2=1,next,sum=0; scanf("%d",&n);
 for(i=1;i<=n;i++){
 if(i%2==0) sum+=t1;
 next=t1+t2; t1=t2; t2=next;
 }
 printf("Sum=%lld",sum);
 return 0;
}
