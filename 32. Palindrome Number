#include <stdio.h>
int main(){
 int n,rev=0,rem,temp; scanf("%d",&n); temp=n;
 for(;temp!=0;temp/=10){ rem=temp%10; rev=rev*10+rem; }
 if(rev==n) printf("Palindrome"); else printf("Not Palindrome");
 return 0;
}
