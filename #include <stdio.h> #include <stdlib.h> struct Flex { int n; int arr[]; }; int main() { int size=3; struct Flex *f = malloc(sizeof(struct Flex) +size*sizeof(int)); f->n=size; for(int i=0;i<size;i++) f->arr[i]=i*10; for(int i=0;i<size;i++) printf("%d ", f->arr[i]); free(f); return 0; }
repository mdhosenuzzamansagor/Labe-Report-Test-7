#include <stdio.h>
int main() {
FILE *fp = tmpfile(); if(fp==NULL) {
printf("Cannot create temp 
file\n"); return 1; 
} 
fprintf(fp,"Hello World");
rewind(fp);
char str[50];
fscanf(fp,"%s", str); printf("%s\n", str);
fclose(fp); 
return 0; 
}
