#include <stdio.h>
#include <string.h>
int main() { 
char a[100],b[100]; 
scanf("%s %s", a,b); 
int res=strcmp(a,b);
if(res==0) printf("Equal\n"); 
else if(res<0)
printf("First is smaller\n"); 
else
printf("First is greater\n");
return 0;
}
