#include <stdio.h>
#include <string.h>
int main() {
char str1[200], 
str2[100];
int pos;
scanf("%s %s %d",
str1,str2,&pos); 
char temp[200]; 
strcpy(temp,str1+pos);
str1[pos]='\0';
strcat(str1,str2);
strcat(str1,temp); 
printf("Result: %s\n", str1);
return 0; 
}
