#include <stdio.h>
#include <string.h>
int main() { char name[100]; fgets(name,100,stdin); if(name[0]!=' ')
printf("%c", name[0]); for(int i=1;i<strlen(name);i++) if(name[i-1]==' ' &&
name[i]!='\n') printf("%c", name[i]); return 0; }
